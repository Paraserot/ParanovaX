export default function VersionPage() {
  // Ye page bas API hit karne ke liye helper text dega
  return (
    <main style={{padding: 24}}>
      <h1>Version Probe</h1>
      <p>API: <code>/api/versions</code> open karo — wahi par actual Next version dikhega.</p>
    </main>
  );
}
