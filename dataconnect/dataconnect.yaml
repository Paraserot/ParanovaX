specVersion: "v1"
serviceId: "firebase-project"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "firebase-project-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
